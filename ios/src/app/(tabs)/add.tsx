// The + in the middle of the tab bar. It's a disabled tab, so tapping it never lands here: the tab bar's tabPress
// listener opens the composer instead. This screen only exists because every tab needs a route.
export default function Add() {
  return null
}
